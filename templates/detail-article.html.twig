{% extends 'base.html.twig' %}

{% block main %}
<main id="article-detail">
    <article class="article-container">
        <h1 class="article-title">{{ article.title }}</h1>
        <p class="article-description">{{ article.description }}</p>
        <div><img src="{{ article.image }}" alt="Image de l'article" class="article-image"></div>
        <p class="article-content">{{ article.content }}</p>
    <a href="{{ path('delete-article', {'id' : article.id}) }}"><button class="delete-article-btn" onclick="confirmDeletion('{{ article.id }}')">Supprimer l'article</button></a>
        <button class="update-article-btn">Modifier l'article</button>
    </article>
</main>
{% endblock main %}