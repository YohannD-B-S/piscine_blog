{% extends 'base.html.twig' %}

{% block main %}
<section class="articles-container">
{% for category in categories %}
<a href="{{ path('detail-category', {'id' : category.id}) }}"><article class="article-preview">
        <h2 class="article-title">{{ category.title }}</h2>
    </article>
{% endfor %}
</section>
</main>
{% endblock main %}